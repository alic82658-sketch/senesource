/**
 * @senesource/domain — modèle de domaine NEUTRE.
 * Aucune dépendance runtime, aucun framework (ni Astro, ni Sanity, ni Expo,
 * ni bibliothèque de validation). Types purs partagés par toutes les surfaces.
 */
export * from './dossier';
